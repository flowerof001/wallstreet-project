import 'package:flutter/material.dart';

/// WallstreetProject 色彩系统
/// A股：涨红跌绿
class AppColors {
  // 背景色
  static const Color bgDark = Color(0xFF182738);
  static const Color bgDarker = Color(0xFF0A2740);

  // 主色系
  static const Color primaryDeep = Color(0xFF0A2E67);
  static const Color primary = Color(0xFF003EA5);
  static const Color primaryLight = Color(0xFF5DA3F3);
  static const Color primaryPale = Color(0xFFBADBFF);

  // 绿色系（A股跌）
  static const Color greenDark = Color(0xFF207700);
  static const Color green = Color(0xFF2FAC00);

  // 红色系（A股涨）
  static const Color redDark = Color(0xFFC13636);
  static const Color red = Color(0xFF5757);

  // 警告色
  static const Color warningDark = Color(0xFFD57800);
  static const Color warning = Color(0xFFCB300);

  // 文本色
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8899AA);
  static const Color textMuted = Color(0xFF556677);

  // 卡片背景
  static const Color cardBg = Color(0xFF1C3045);
  static const Color cardBorder = Color(0xFF2A4058);

  // 分隔线
  static const Color divider = Color(0xFF2A3F55);
}
